<Window x:Class="ApocalypseLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Apocalypse Launcher" Height="630" Width="900"
        WindowStyle="None" ResizeMode="CanResizeWithGrip" AllowsTransparency="True"
        Background="Transparent"
        Icon="pack://application:,,,/server_ico.ico"
        MouseDown="Window_MouseDown" Loaded="Window_Loaded" Closing="Window_Closing">

    <Window.Resources>
        <Style x:Key="GlowButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="6" Background="{TemplateBinding Background}" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Border.Effect>
                                <DropShadowEffect Color="#d4af37" BlurRadius="0" ShadowDepth="0" Opacity="0"/>
                            </Border.Effect>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#d4af37" BlurRadius="15" ShadowDepth="0" Opacity="0.8"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowControlButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="Tag" Value="Close"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#FF0000"/>
                            </MultiTrigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsMouseOver" Value="True"/>
                                    <Condition Property="Tag" Value="Minimize"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="border" Property="Background" Value="#444444"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GoldComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#1a1a1a"/>
            <Setter Property="Foreground" Value="#d4af37"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Grid.Column="2" Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border" Background="#1a1a1a" BorderBrush="#444444" BorderThickness="1" CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <ContentPresenter Grid.Column="0"/>
                                                <Path x:Name="Arrow" Grid.Column="1" Fill="#d4af37" HorizontalAlignment="Center" VerticalAlignment="Center" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="BorderBrush" Value="#d4af37"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="8,4,25,4" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#1a1a1a" BorderThickness="1" BorderBrush="#d4af37" CornerRadius="4"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GoldComboBoxItemStyle" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#bbbbbb"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#333333"/>
                                <Setter Property="Foreground" Value="#d4af37"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#d4af37"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LanguageButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FF0000"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NewsScrollBarThumbStyle" TargetType="Thumb">
            <Setter Property="MinHeight" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#d4af37" CornerRadius="4" Opacity="0.85"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NewsScrollBarStyle" TargetType="ScrollBar">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid SnapsToDevicePixels="True">
                            <Border Background="#222222" CornerRadius="5" Margin="0,2,0,2"/>
                            <Track x:Name="PART_Track" Margin="2"
                                Orientation="{TemplateBinding Orientation}"
                                IsDirectionReversed="False"
                                   Minimum="{Binding Minimum, RelativeSource={RelativeSource TemplatedParent}}"
                                   Maximum="{Binding Maximum, RelativeSource={RelativeSource TemplatedParent}}"
                                   Value="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}"
                                   ViewportSize="{Binding ViewportSize, RelativeSource={RelativeSource TemplatedParent}}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton IsEnabled="False" Opacity="0" Height="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource NewsScrollBarThumbStyle}"/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton IsEnabled="False" Opacity="0" Height="0"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=ScrollViewer}}" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="8" Background="#121212" BorderBrush="#333333" BorderThickness="1" ClipToBounds="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="90"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>

                        <Image Name="BgImage"
                                         Source="/Logo_Server.png"
                                     Grid.Row="1"
                                     Grid.RowSpan="3"
                                     Stretch="UniformToFill"
                                     Opacity="0.4"
                                     HorizontalAlignment="Left"
                                     Margin="-200,0,0,0"
                                     VerticalAlignment="Center"
                                     RenderOptions.BitmapScalingMode="HighQuality"/>

            <Grid Grid.Row="0" Background="#1a1a1a">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <TextBlock Text="APOCALYPSE" Foreground="#d4af37" FontWeight="Bold" FontSize="14" Margin="0,0,5,0"/>
                    <TextBlock Text="LAUNCHER" Foreground="#888888" FontWeight="Normal" FontSize="14"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,10,0">
                    <Button Name="BtnEng" Content="ENG" Click="BtnEng_Click" Width="50" Height="25" 
                        Foreground="White" FontWeight="Bold" Style="{StaticResource LanguageButtonStyle}"/>
                    <TextBlock Text="|" Foreground="#333333" VerticalAlignment="Center"/>
                    <Button Name="BtnEsp" Content="ESP" Click="BtnEsp_Click" Width="50" Height="25" 
                        Foreground="Gray" FontWeight="Bold" Style="{StaticResource LanguageButtonStyle}"/>

                    <Button Content="—" Tag="Minimize" Click="BtnMinimize_Click" Width="30" Height="30" Margin="20,0,0,0"
                            Style="{StaticResource WindowControlButtonStyle}"/>
                    <Button Content="✕" Tag="Close" Click="BtnClose_Click" Width="30" Height="30" Margin="5,0,0,0"
                            Style="{StaticResource WindowControlButtonStyle}"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="320"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="50,0,0,0">
                    <TextBlock Name="TxtSubtitle" Text="" Foreground="#d4af37" FontSize="28" FontWeight="Bold" Opacity="0.8">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="10" ShadowDepth="2"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>

                <Border Grid.Column="1" Margin="10,20,20,20" Background="#CC000000" BorderBrush="#444444" BorderThickness="1" CornerRadius="4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="30"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="NEWS &amp; UPDATES" Foreground="#d4af37" FontWeight="Bold" 
                                   VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="12">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="#d4af37" BlurRadius="5" ShadowDepth="0" Opacity="0.5"/>
                            </TextBlock.Effect>
                        </TextBlock>

                        <ScrollViewer Name="NewsScrollViewer" Grid.Row="1" Margin="10" VerticalScrollBarVisibility="Auto"
                                      PreviewMouseWheel="NewsScrollViewer_PreviewMouseWheel">
                            <ScrollViewer.Resources>
                                <Style TargetType="ScrollBar" BasedOn="{StaticResource NewsScrollBarStyle}"/>
                            </ScrollViewer.Resources>
                            <TextBlock Name="TxtNewsContent" 
                                     Text="Loading news..." 
                                     Foreground="#bbbbbb" 
                                     FontFamily="Consolas, Courier New, Monospace" 
                                     FontSize="12" 
                                     TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>

                <!-- Resolution and Fullscreen selectors on the left side -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,20,0">
                    <TextBlock Name="TxtResolutionLabel" Text="RESOLUTION:" Foreground="#888888" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Name="CmbResolution" Width="120" Style="{StaticResource GoldComboBoxStyle}" ItemContainerStyle="{StaticResource GoldComboBoxItemStyle}" SelectionChanged="CmbResolution_SelectionChanged">
                        <ComboBoxItem Content="1024 x 768" Tag="0"/>
                        <ComboBoxItem Content="800 x 600" Tag="1"/>
                        <ComboBoxItem Content="640 x 480" Tag="2"/>
                    </ComboBox>
                    <StackPanel Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                        <CheckBox Name="ChkFullscreen" VerticalAlignment="Center" Checked="ChkFullscreen_Changed" Unchecked="ChkFullscreen_Changed">
                            <CheckBox.Style>
                                <Style TargetType="CheckBox">
                                    <Setter Property="Foreground" Value="#888888"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="CheckBox">
                                                <Border x:Name="checkBorder" Width="16" Height="16" BorderBrush="#444444" BorderThickness="1" Background="#1a1a1a" CornerRadius="2" VerticalAlignment="Center">
                                                    <Path x:Name="checkMark" Data="M 2 6 L 6 10 L 12 2" Stroke="#d4af37" StrokeThickness="2" Visibility="Collapsed" Margin="1"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                                        <Setter TargetName="checkBorder" Property="BorderBrush" Value="#d4af37"/>
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="checkBorder" Property="BorderBrush" Value="#d4af37"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                        <TextBlock Name="TxtFullscreenLabel" Text="FULLSCREEN" Foreground="#888888" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" Margin="6,0,0,0" Cursor="Hand" MouseLeftButtonDown="TxtFullscreenLabel_Click"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,20,0">

                    <DockPanel LastChildFill="False" Margin="5,0,5,5" HorizontalAlignment="Stretch">
                        <TextBlock Name="TxtStatus" Text="Initializing..." Foreground="#dddddd" FontSize="12" HorizontalAlignment="Left">
                            <TextBlock.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="1" Opacity="0.8"/>
                            </TextBlock.Effect>
                        </TextBlock>
                        <TextBlock Name="TxtPercentage" Text="0%" DockPanel.Dock="Right" Foreground="#d4af37" FontSize="12" FontWeight="Bold" Visibility="Collapsed"/>
                    </DockPanel>

                    <ProgressBar Name="PbDownload" Height="6" Background="#222222" BorderThickness="0" Foreground="#d4af37" Visibility="Collapsed" Margin="5,0,5,5"/>

                    <Button Name="BtnPlay" Content="PLAY" Click="BtnPlay_Click"
                            FontSize="24" FontWeight="Black" FontFamily="Arial"
                            Background="#d4af37" Foreground="#1a1a1a"
                            Height="50" Margin="0" IsEnabled="False" Cursor="Hand"
                            Style="{StaticResource GlowButtonStyle}"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="3" Background="#0d0d0d">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Launcher v1.0" Grid.Column="0" Foreground="#666666" 
                           VerticalAlignment="Center" Margin="10,0,0,0" FontSize="11"/>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,10,0" VerticalAlignment="Center">
                    <TextBlock Text="SERVER STATUS: " Foreground="#888888" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Name="TxtServerStatus" Text="CHECKING..." Foreground="Orange" FontSize="11" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>